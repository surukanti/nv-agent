# NV-Agent — development & testing requirements
# Install alongside runtime deps: pip install -r requirements.txt -r requirements-dev.txt

pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27
ruff>=0.5
mypy>=1.10
pre-commit>=3.7
flake8
pylint
